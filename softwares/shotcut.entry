#!/usr/bin/env bash

name="Shotcut"
package_name="shotcut"
description="クロスプラットホームのビデオエディター"
run_preparing=false


install () {
    pacman -S --noconfirm ${package_name}
}

uninstall () {
    pacman -Rsn --noconfirm ${package_name}
}