name="Adapta-Theme" ===改行コード=== package_name="htop" ===改行コード=== description="マテリアルデザインに基づいたテーマパック" ===改行コード=== run_preparing=false ===改行コード=== function install () { ===改行コード=== apt install -y $package_name ===改行コード=== } ===改行コード=== function uninstall () { ===改行コード=== apt purge -y ${package_name} && apt autoremove ===改行コード=== }
name="Adapta-Theme" ===改行コード=== package_name="gparted" ===改行コード=== description="マテリアルデザインに基づいたテーマパック" ===改行コード=== run_preparing=false ===改行コード=== function install () { ===改行コード=== apt install -y $package_name ===改行コード=== } ===改行コード=== function uninstall () { ===改行コード=== apt purge -y ${package_name} && apt autoremove ===改行コード=== }
